box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.74.3"
        config: config.yaml
       
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: hugo-wercker.ig.nore.me
        basedir: public
